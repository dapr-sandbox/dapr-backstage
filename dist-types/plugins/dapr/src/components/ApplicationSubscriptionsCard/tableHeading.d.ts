import { TableColumn } from '@backstage/core-components/index';
import { Subscription } from '../../types';
export declare const columns: TableColumn<Subscription>[];
export declare const useStyles: (props?: any) => import("@material-ui/styles").ClassNameMap<"empty">;
